# Define an empty list to store tasks                      tasks = []                                                                                                            # Function to add a task to the list                       def add_task():                                                task = input("Enter a task: ")                             tasks.append(task)                                         print("Task added successfully!")                                                                                 # Function to mark a task as completed                     def complete_task():                                           if len(tasks) == 0:                                            print("No tasks in the list!")                         else:                                                          print("Tasks:")                                            for index, task in enumerate(tasks):
            print(f"{index + 1}. {task}")                  
        task_index = int(input("Enter the task number to mark as completed: "))                                               if task_index < 1 or task_index > len(tasks):                  print("Invalid task number!")
        else:
            completed_task = tasks.pop(task_index - 1)
